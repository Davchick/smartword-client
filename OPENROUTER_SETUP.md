# Настройка OpenRouter API для SmartWord

## 🎯 Стратегия: Максимум бесплатных запросов

OpenRouter даёт **1,000 бесплатных запросов/день** на каждый аккаунт с пополнением **$10+**.

**Выгода:**
- 1 аккаунт × $10 = 1,000 запросов/день
- 3 аккаунта × $10 = **3,000 запросов/день** 🚀
- 5 аккаунтов × $10 = **5,000 запросов/день**

---

## 📋 Шаг 1: Создание аккаунтов OpenRouter

### Для каждого аккаунта:

1. **Регистрация:**
   - Зайди на https://openrouter.ai
   - Нажми **Sign In** (можно через Google/GitHub)
   - Создай новый аккаунт

2. **Пополнение на $10:**
   - Перейди в **Credits** (верхнее меню)
   - Нажми **Add Credits**
   - Выбери **Use crypto** (или карту)
   - Отправь **$10 в USDT/BTC/ETH**
   - Минимальное пополнение: ~$5-10

3. **Получение API ключа:**
   - Перейди в **Keys**
   - Нажми **Create New Key**
   - Скопируй ключ (начинается с `sk-or-...`)

4. **Повтори** для нужного количества аккаунтов (рекомендуется 3-5)

---

## 🔧 Шаг 2: Настройка сервера

### server/.env

```env
# OpenRouter API ключи — перечисли через запятую (без пробелов!)
# Пример для 3 аккаунтов:
OPENROUTER_API_KEYS=sk-or-v1-abc123,sk-or-v1-def456,sk-or-v1-ghi789
```

**Важно:**
- ✅ Ключи через запятую, **без пробелов**
- ✅ Минимум 1 ключ, лучше 3-5
- ✅ Каждый ключ должен быть с аккаунта с $10+ балансом

---

## 🚀 Шаг 3: Запуск сервера

```bash
cd server
npm run dev
```

---

## 📊 Шаг 4: Проверка работы

### 1. Тестовый запрос

Отправь сообщение в чат приложения — должно работать.

### 2. Проверка использования

```bash
GET /api/chat/usage
Authorization: Bearer <твой JWT токен>
```

**Ответ:**
```json
{
  "stats": [
    {
      "key": "sk-or-v...",
      "usage": 150,
      "limit": 1000,
      "remaining": 850
    },
    {
      "key": "sk-or-v...",
      "usage": 0,
      "limit": 1000,
      "remaining": 1000
    }
  ]
}
```

---

## 🔄 Как работает fallback система

```
Запрос к AI
    │
    ▼
┌───────────────────────────────────┐
│ Ключ #1 (usage: 500/1000)         │
│ ├─ Успех → Возврат ответа         │
│ └─ Ошибка 429 → Следующий ключ    │
└───────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────┐
│ Ключ #2 (usage: 480/1000)         │
│ ├─ Успех → Возврат ответа         │
│ └─ Ошибка 429 → Следующий ключ    │
└───────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────┐
│ Ключ #3 (usage: 920/1000)         │
│ ├─ Успех → Возврат ответа         │
│ └─ Ошибка 429 → Ключ #1 (сброс)   │
└───────────────────────────────────┘
```

**Автоматическое переключение:**
- Если ключ исчерпал 1,000 запросов/день → следующий ключ
- Если ключ вернул ошибку 429 → следующий ключ
- Если ключ вернул 401/403 → следующий ключ
- Если все ключи исчерпаны → ошибка

---

## 💰 Модель

Система использует **одну модель**:

### **arcee-ai/trinity-large-preview:free**

| Параметр | Значение |
|----------|----------|
| **Модель** | `arcee-ai/trinity-large-preview:free` |
| **Тип** | Бесплатная (:free) |
| **Качество** | ⭐⭐⭐⭐⭐ |
| **Мультязычность** | Отличная |
| **Контекст** | 131K токенов |

**Почему она:**
- ✅ Полностью бесплатная (в рамках 1,000 запросов/день)
- ✅ Высокое качество диалогов
- ✅ Хорошая мультязычность (русский, английский, арабский, китайский)
- ✅ Достаточно 131K контекста для длинных бесед

---

### 🔄 Fallback между ключами

Система **автоматически переключается** между ключами при исчерпании лимитов:

```
Запрос к AI
    │
    ▼
Ключ #1 (usage: 950/1000)
    │
    ├─ Успех → Возврат ответа
    └─ 429 (лимит) → Ключ #2
                       │
                       ├─ Успех → Возврат ответа
                       └─ 429 (лимит) → Ключ #3
                                          │
                                          ├─ Успех → Возврат ответа
                                          └─ Ошибка → Возврат ошибки
```

**Важно:** Модель всегда одна и та же — `arcee-ai/trinity-large-preview:free`

---

## 📈 Ожидаемые лимиты

| Аккаунтов | $ всего | Запросов/день | Запросов/месяц |
|-----------|---------|---------------|----------------|
| **1** | $10 | 1,000 | 30,000 |
| **3** | $30 | 3,000 | 90,000 |
| **5** | $50 | 5,000 | 150,000 |

### Для SmartWord:

| Пользователей/день | Запросов/день | Нужно аккаунтов |
|-------------------|---------------|-----------------|
| **50** | ~500 | 1 аккаунт ($10) |
| **100** | ~1,000 | 1 аккаунт ($10) |
| **200** | ~2,000 | 2 аккаунта ($20) |
| **300** | ~3,000 | 3 аккаунта ($30) |
| **500** | ~5,000 | 5 аккаунтов ($50) |

---

## ⚠️ Troubleshooting

### Ошибка: "No OpenRouter API keys configured"

**Причина:** Не указан `OPENROUTER_API_KEYS` в `.env`

**Решение:** Добавь ключи и перезапусти сервер

---

### Ошибка: "All OpenRouter API keys exhausted"

**Причина:** Все ключи исчерпали 1,000 запросов/день

**Решение:**
1. Подожди до следующего дня (лимиты сбрасываются каждые 24 часа)
2. Или добавь ещё аккаунтов с $10+

---

### Ошибка: "401 Unauthorized" или "403 Forbidden"

**Причина:** Неверный API ключ

**Решение:**
1. Проверь ключ в OpenRouter Console
2. Убедись, что аккаунт пополнен на $10+
3. Пересоздай ключ если нужно

---

### Ошибка: "429 Too Many Requests"

**Причина:** Превышен лимит запросов в минуту (RPM)

**Решение:**
- Система автоматически переключится на следующий ключ
- Если все ключи исчерпаны — подожди немного

---

## 🎯 Оптимизация расходов

### 1. Используй бесплатные модели

В коде уже настроено использование `:free` моделей:
```javascript
'meta-llama/llama-3.1-70b-instruct:free'
```

### 2. Кэширование ответов

Для частых запросов (например, перевод одинаковых слов) — добавь кэш.

### 3. Мониторинг использования

Проверяй `/api/chat/usage` регулярно:
- Если ключ приближается к 1,000 — готовь следующий
- Если все ключи ~900 — добавляй новый аккаунт

---

## 🔗 Полезные ссылки

| Сервис | Ссылка |
|--------|--------|
| **OpenRouter** | https://openrouter.ai |
| **API Keys** | https://openrouter.ai/keys |
| **Бесплатные модели** | https://openrouter.ai/openrouter/free |
| **Цены** | https://openrouter.ai/pricing |
| **Документация** | https://openrouter.ai/docs |

---

## 💡 Советы

1. **Храни ключи в безопасности:**
   - Не коммить `.env` в git
   - Используй разные email для аккаунтов

2. **Мониторь баланс:**
   - Проверяй Credits в каждом аккаунте
   - $10 не сгорают, но бесплатные 1,000 запросов обновляются каждые 24 часа

3. **Автоматизируй мониторинг:**
   - Настрой уведомления при 80% использовании ключа
   - Добавь эндпоинт `/api/chat/usage` в админ-панель

4. **Масштабирование:**
   - При росте >500 пользователей/день — добавляй аккаунты
   - 10 аккаунтов = 10,000 запросов/день = ~$100

---

## 🎉 Итог

**При 3 аккаунтах по $10:**
- ✅ 3,000 бесплатных запросов/день
- ✅ ~90,000 запросов/месяц
- ✅ Хватит на ~300 пользователей/день
- ✅ Общие затраты: $30 (единоразово)

**Это в 10-20 раз дешевле** чем облачные API типа GigaChat! 🚀
